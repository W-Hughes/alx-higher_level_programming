#!/usr/bin/node
// This script prints 3 lines of strings using an array of strings and a loop.
const texts = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (const text of texts) {
  console.log(text);
}
